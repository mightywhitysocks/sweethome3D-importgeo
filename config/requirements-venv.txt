# Substitut pip (sans conda) de config/environment.yml, pour lancer src/*.py
# directement (verif.py, build_home.py, bati.py, ...) dans un venv classique --
# utile quand l'env conda `sitegeo` n'est pas disponible (ex. session Claude
# Code distante, cf. CLAUDE.md §Environnement > Session Claude Code distante).
#
#   python3 -m venv .venv && .venv/bin/pip install -r config/requirements-venv.txt
#   .venv/bin/python src/verif.py
#
# Valide de bout en bout dans ce mode (imports + execution reelle, pas
# seulement l'installation) : verif.py, build_home.py, bati.py (incluant
# roof_lidar.py). Un seul manque connu et non bloquant pour build_home.py :
# gdal_contour.exe (courbes.py) n'a pas d'equivalent pip installable (binaire
# GDAL Windows).
#
# Versions figees a celles reellement testees (pas un simple report des
# pins conda de environment.yml : le python3 par defaut de ce type de
# conteneur est 3.11, pas 3.12 -- numpy 2.5.* exige >=3.12 et n'installe pas).
numpy~=2.4.6
scipy~=1.17.1
shapely~=2.1.2
geopandas~=1.1.4
owslib~=0.35.0
rasterio~=1.4.4
scikit-image~=0.26.0
networkx~=3.6.1
pyproj~=3.7.2
pillow~=12.3.0
pyvista~=0.48.4
laspy~=2.7.0
requests~=2.34.2
javaobj-py3==0.6.1
