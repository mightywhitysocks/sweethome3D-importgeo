# Substitut pip (sans conda) de config/environment.yml, pour lancer src/*.py
# directement (verif.py, build_home.py, bati.py, ...) dans un venv classique --
# utilise par run.sh (Linux/macOS) et par l'image Docker de generation.yml.
#
#   python3 -m venv .venv && .venv/bin/pip install -r config/requirements-venv.txt
#   .venv/bin/python src/verif.py
#
# gdal_contour (courbes.py) n'a pas d'equivalent pip installable : requiert
# le paquet systeme gdal-bin (apt) / gdal (Homebrew), deja present dans
# l'image Docker de generation.yml -- cf. README.md > Prerequis pour un venv
# local.
#
# Versions harmonisees avec config/environment.yml (memes versions cote
# conda Windows et pip Linux/macOS/Docker, cf. CLAUDE.md) -- besoin de
# Python >=3.12 (numpy 2.5.* l'exige). Python 3.14 recommande (image Docker
# et environment.yml alignes dessus).
numpy~=2.5.2
scipy~=1.18.1
shapely~=2.1.2
geopandas~=1.1.4
owslib~=0.36.0
rasterio~=1.5.1
scikit-image~=0.26.0
networkx~=3.6.1
pyproj~=3.7.2
pillow~=12.3.0
pyvista~=0.48.4
# backend de decompression LAZ -- pip laspy ne l'embarque pas (contrairement
# au paquet conda-forge), sinon LaspyException "No LazBackend selected" a la
# lecture d'une dalle .copc.laz
laspy~=2.7.0
lazrs~=0.8.2
requests~=2.34.2
javaobj-py3==0.6.1
